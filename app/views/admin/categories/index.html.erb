
<h1> 類別清單 </h1>

<table class="table table-striped table-bordered">
<thead>
    <tr>
    <th> #</th>
    <th> 類別名稱</th>
    <th>  </th>
    </tr>
</thead>
<tbody>
    <% @categories.each do |category| %>
      
        <tr>
            <td><%= category.id %></td>
            <td><%= category.name %></td>
            <td><%= link_to "Edit", admin_edit_category_path(category), class: "btn btn-primary" %></td>
            <td><%= link_to "Delete", admin_delete_category_path(category), class: "btn btn-default" %></td>
        </tr>

    <% end %>
    
</tbody>
</table>